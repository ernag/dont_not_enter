pntcalc is a python module that calculates activity points for a set of given set of accelerometer data.

The data is expected to contain information for all three channels of the accel with values ranging from -128 to 127 inclusive.

For a short examples on how to use the module's only accessible function, calculate, see example.py

to install the module, type

$ python setup.py build
$ python setup.py install
